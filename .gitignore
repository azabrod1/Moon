node_modules/
dist/
.vite/
*.local
.claude/*
!.claude/skills/
# the shared permission allowlist: tracked so every checkout and every
# cloud session starts with it (settings.local.json stays machine-local)
!.claude/settings.json
.chrome-*/
.edge-headless-profile/
browser-*.png
explore-*.png
orbit-details-browser-check.png
preview-*.log
vite-*.log
http-test*.log
.moon-data-cache/
# internal planning docs / mockups / QA scratch — local-only
planning/
