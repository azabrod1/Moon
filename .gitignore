node_modules/
dist/
.vite/
*.local
.claude/*
!.claude/skills/
# the shared allowlist: tracked so cloud sessions, which never see
# settings.local.json, do not start with zero permission rules
!.claude/settings.json
.chrome-*/
.edge-headless-profile/
browser-*.png
explore-*.png
orbit-details-browser-check.png
preview-*.log
vite-*.log
http-test*.log
.moon-data-cache/
# internal planning docs / mockups / QA scratch — local-only
planning/
